# nginx (docker-compose) fronting Kong — used for auth-service GraphQL calls
VITE_API_BASE_URL=http://localhost

# Placeholder until a real payment-service exists — see src/lib/paymentClient.ts
VITE_PAYMENT_API_BASE_URL=http://localhost
